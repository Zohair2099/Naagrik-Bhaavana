rules_version = '2';

// Warning: These rules are permissive and intended for development and
// hackathon environments. They allow public read and write access for authenticated users.
// For a production application, you must implement stricter security rules.
// https://firebase.google.com/docs/storage/security
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
